// панель.код — среда разработки языка Сфера. Окно с боковой панелью
// вкладок (Обзор / Пакеты / Документация) и основной областью справа —
// каждая кнопка внутри реально вызывает компилятор/менеджер пакетов
// через выполнить_команду (не имитация — настоящий запуск процессов).
подключить графика;

функция текущая_версия() -> текст {
    вернуть "0.2.0";
}

// --- Пути и самоустановка (отработано и проверено ранее) ---

функция выбрать_путь_шрифта() -> текст {
    текст macos_путь = "/System/Library/Fonts/Supplemental/Arial Unicode.ttf";
    результат<текст> проверка_macos = прочитать_файл(macos_путь);
    если (проверка_macos.успешно) {
        вернуть macos_путь;
    }

    текст linux_путь = "/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf";
    результат<текст> проверка_linux = прочитать_файл(linux_путь);
    если (проверка_linux.успешно) {
        вернуть linux_путь;
    }

    вернуть linux_путь;
}

внешняя функция получить_переменную_окружения(текст имя) -> текст как "getenv";

функция домашняя_папка() -> текст {
    вернуть получить_переменную_окружения("HOME");
}

// НОВОЕ: путь установки теперь ПАРАМЕТР, а не жёстко зашитое значение —
// пользователь выбирает его сам через системный диалог (см.
// выбрать_папку_через_диалог ниже) при нажатии "Установить". По
// умолчанию (если пользователь ничего не выбирал) используется прежнее
// значение ~/.sfera — см. папка_установки_по_умолчанию.
функция папка_установки_по_умолчанию() -> текст {
    вернуть домашняя_папка() + "/.sfera";
}

функция путь_установленного_sfera(текст базовая_папка) -> текст {
    вернуть базовая_папка + "/bin/запустить";
}

функция путь_установленного_сфера_пакеты(текст базовая_папка) -> текст {
    вернуть базовая_папка + "/bin/сфера_пакеты";
}

функция путь_установленного_примера(текст базовая_папка) -> текст {
    вернуть базовая_папка + "/share/привет.код";
}

функция путь_файла_версии(текст базовая_папка) -> текст {
    вернуть базовая_папка + "/share/версия.txt";
}

функция путь_встроенного_бандла() -> текст {
    вернуть "/Applications/Панель Сферы.app/Contents/Resources/bin";
}

функция сфера_уже_установлена(текст базовая_папка) -> верно {
    результат<текст> проверка = прочитать_файл(путь_установленного_sfera(базовая_папка));
    вернуть проверка.успешно;
}

// НОВОЕ: системный диалог выбора папки — macOS-нативный, через
// "osascript" (тот же приём, что и остальная панель уже использует для
// запуска процессов: команда пишет результат в файл, мы читаем файл, а
// не пытаемся разобрать вывод напрямую из живого процесса). "choose
// folder" — стандартный, встроенный в macOS диалог выбора папки, без
// сторонних зависимостей.
//
// ⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (нет доступа к настоящему macOS здесь) — как и
// с Windows-установщиком ранее, первая реальная проверка — по факту
// присланной ошибки, если она возникнет.
функция выбрать_папку_через_диалог() -> результат<текст> {
    текст лог = "/tmp/sfera_панель_выбор_папки.log";
    текст команда = "osascript -e 'POSIX path of (choose folder with prompt \"Выберите папку для установки языка Сфера\")' > " + лог + " 2>/dev/null";
    результат<целое> итог = выполнить_команду(команда);
    // Пользователь нажал "Отмена" в диалоге — osascript возвращает
    // ненулевой код (это НЕ ошибка запуска команды, а честный,
    // ожидаемый отказ пользователя, обрабатывается отдельно, не как
    // "не удалось выполнить команду").
    если (!итог.успешно || итог.значение != 0) {
        вернуть ошибка("отменено пользователем");
    }

    результат<текст> содержимое = прочитать_файл(лог);
    если (!содержимое.успешно) {
        вернуть ошибка("не удалось прочитать выбранный путь");
    }

    // "choose folder" всегда возвращает POSIX-путь с завершающим "/" и
    // переводом строки в конце — убираем оба, прежде чем использовать
    // как обычный путь для конкатенации.
    текст путь = содержимое.значение;
    результат<целое> позиция_переноса = найти(путь, "\n");
    если (позиция_переноса.успешно) {
        результат<текст> обрезанный = подстрока(путь, 0, позиция_переноса.значение);
        если (обрезанный.успешно) {
            путь = обрезанный.значение;
        }
    }
    если (длина(путь) > 0) {
        результат<текст> последний_символ_результат = подстрока(путь, длина(путь) - 1, 1);
        если (последний_символ_результат.успешно && последний_символ_результат.значение == "/") {
            результат<текст> без_слеша = подстрока(путь, 0, длина(путь) - 1);
            если (без_слеша.успешно) {
                путь = без_слеша.значение;
            }
        }
    }

    вернуть успех(путь + "/.sfera");
}

функция установить_сферу(текст базовая_папка) -> верно {
    текст папка_bin = базовая_папка + "/bin";
    текст папка_share = базовая_папка + "/share";
    текст встроенный_bin = путь_встроенного_бандла();

    текст команда = "mkdir -p '" + папка_bin + "' '" + папка_share + "' && "
        + "cp '" + встроенный_bin + "/запустить' '" + папка_bin + "/запустить' && "
        + "cp '" + встроенный_bin + "/сфера_lsp' '" + папка_bin + "/сфера_lsp' && "
        + "cp '" + встроенный_bin + "/сфера_пакеты' '" + папка_bin + "/сфера_пакеты' && "
        + "cp '" + встроенный_bin + "/привет.код' '" + папка_share + "/привет.код' && "
        + "echo '" + текущая_версия() + "' > '" + папка_share + "/версия.txt' && "
        + "chmod +x '" + папка_bin + "/запустить' '" + папка_bin + "/сфера_lsp' '" + папка_bin + "/сфера_пакеты'";

    результат<целое> итог = выполнить_команду(команда);
    вернуть итог.успешно && итог.значение == 0;
}

// НОВОЕ: удаление — обратная операция, полностью убирает папку установки.
функция удалить_сферу(текст базовая_папка) -> верно {
    текст команда = "rm -rf '" + базовая_папка + "'";
    результат<целое> итог = выполнить_команду(команда);
    вернуть итог.успешно && итог.значение == 0;
}

// НОВОЕ: открыть README во внешней программе просмотра — пробуем "open"
// (macOS) и "xdg-open" (Linux) через "||", один из них да сработает.
функция открыть_документацию() -> верно {
    результат<целое> итог = выполнить_команду("open README.md || xdg-open README.md");
    вернуть итог.успешно;
}

// НОВОЕ: захватывает список пакетов В ФАЙЛ (а не в невидимую консоль —
// пользователь, запустивший панель двойным кликом, эту консоль вообще
// не увидит) и возвращает его содержимое одной строкой, с переносами.
функция получить_список_пакетов(текст базовая_папка) -> текст {
    текст лог = "/tmp/sfera_панель_пакеты.log";
    текст команда = "cd /tmp && " + путь_установленного_сфера_пакеты(базовая_папка) + " список > " + лог + " 2>&1";
    выполнить_команду(команда);
    результат<текст> содержимое = прочитать_файл(лог);
    если (содержимое.успешно) {
        вернуть содержимое.значение;
    }
    вернуть "Не удалось получить список пакетов.";
}

// НОВОЕ: проверяет, начинается ли "строка" ровно с "префикс" (в самом
// начале, не где угодно — в отличие от "содержит").
// НОВОЕ (настоящая проверка обновлений через GitHub Releases API — не
// полноценный JSON-парсер, а точечное извлечение конкретных, известных
// полей из фиксированного формата ответа GitHub, тем же уровнем
// прагматизма, что и разбор вывода "сфера_пакеты список" ниже). Ищет
// '"имя_поля":"', возвращает всё до следующей НЕэкранированной кавычки
// (учитывает \" внутри значения — например, в тексте описания релиза).
функция извлечь_json_строковое_поле(текст json, текст имя_поля) -> результат<текст> {
    текст искомое = "\"" + имя_поля + "\":\"";
    результат<целое> позиция_ключа = найти(json, искомое);
    если (!позиция_ключа.успешно) {
        вернуть ошибка("поле '" + имя_поля + "' не найдено в ответе");
    }
    целое начало_значения = позиция_ключа.значение + длина(искомое);

    целое индекс = начало_значения;
    целое конец_значения = 0 - 1;
    пока (индекс < длина(json) && конец_значения == 0 - 1) {
        целое32 байт = байт_текста(json, индекс);
        если (байт == целое32(34)) {
            если (индекс == начало_значения) {
                конец_значения = индекс;
            } иначе {
                целое32 предыдущий_байт = байт_текста(json, индекс - 1);
                если (предыдущий_байт != целое32(92)) {
                    конец_значения = индекс;
                }
            }
        }
        индекс = индекс + 1;
    }

    если (конец_значения == 0 - 1) {
        вернуть ошибка("не удалось найти конец значения поля '" + имя_поля + "'");
    }

    вернуть подстрока(json, начало_значения, конец_значения - начало_значения);
}

// НОВОЕ: базовая разэкранировка JSON-строки — \n в настоящий перенос
// строки (для показа через нарисовать_многострочный_текст), \" в ",
// \\ в \, \r отбрасывается. Байты копируются по одному через подстроку
// (не через байт_текста — та даёт число, не кусок текста) — работает
// корректно и для многобайтовых, кириллических символов: раз они просто
// копируются последовательно, без попытки их интерпретировать.
функция разэкранировать_json_текст(текст исходный) -> текст {
    текст итог = "";
    целое индекс = 0;
    пока (индекс < длина(исходный)) {
        целое32 байт = байт_текста(исходный, индекс);
        если (байт == целое32(92) && индекс + 1 < длина(исходный)) {
            целое32 следующий = байт_текста(исходный, индекс + 1);
            если (следующий == целое32(110)) {
                итог = итог + "\n";
                индекс = индекс + 2;
            } иначе если (следующий == целое32(34)) {
                итог = итог + "\"";
                индекс = индекс + 2;
            } иначе если (следующий == целое32(92)) {
                итог = итог + "\\";
                индекс = индекс + 2;
            } иначе если (следующий == целое32(114)) {
                индекс = индекс + 2;
            } иначе {
                результат<текст> символ = подстрока(исходный, индекс, 1);
                если (символ.успешно) {
                    итог = итог + символ.значение;
                }
                индекс = индекс + 1;
            }
        } иначе {
            результат<текст> символ = подстрока(исходный, индекс, 1);
            если (символ.успешно) {
                итог = итог + символ.значение;
            }
            индекс = индекс + 1;
        }
    }
    вернуть итог;
}

// НОВОЕ: настоящая проверка обновлений через GitHub Releases API —
// заменяет прежний "фейковый сервер обновлений" (file:///tmp/...).
// Скачивает JSON последнего релиза через curl (в языке пока нет
// HTTPS/TLS — GitHub API доступен только по HTTPS, поэтому используется
// уже установленная в системе curl, тем же приёмом, что и остальная
// панель), извлекает номер версии и полный текст описания изменений.
// НОВОЕ: релиз на GitHub может содержать сборки сразу под несколько ОС
// (macOS/Windows/Linux/Android) — один и тот же номер версии не
// обязательно значит, что ЭТОЙ конкретной панели есть что скачивать
// (например, версия вышла с исправлением только под Android). Файлы
// релиза (assets) должны называться по условию, включающему метку
// платформы (например "Панель-Сферы-macOS.zip") — эта функция ищет её
// среди имён файлов, не просто сравнивает номер версии.
//
// ⚠️ Честно: эта конкретная панель (osascript, .app-бандл) существует
// только для macOS сейчас — метка ниже отражает именно её. Панели для
// других ОС, когда появятся, будут иметь свою.
функция метка_текущей_платформы() -> текст {
    вернуть "macOS";
}

// Вырезает СОДЕРЖИМОЕ JSON-массива по имени поля (например "assets") —
// от открывающей "[" до СООТВЕТСТВУЮЩЕЙ закрывающей "]", считая глубину
// вложенности (в массиве объектов assets вложенных [] не бывает, но
// подсчёт глубины на всякий случай честнее, чем поиск первой попавшейся
// "]"). Та же, уже принятая для этого файла степень упрощения — не
// полноценный JSON-парсер, точечное извлечение для известного,
// фиксированного формата ответа GitHub.
функция извлечь_json_массив(текст json, текст имя_поля) -> результат<текст> {
    текст искомое = "\"" + имя_поля + "\":[";
    результат<целое> позиция = найти(json, искомое);
    если (!позиция.успешно) {
        вернуть ошибка("поле '" + имя_поля + "' не найдено");
    }
    целое начало = позиция.значение + длина(искомое);

    целое глубина = 1;
    целое индекс = начало;
    целое конец = 0 - 1;
    пока (индекс < длина(json) && конец == 0 - 1) {
        целое32 байт = байт_текста(json, индекс);
        если (байт == целое32(91)) {
            глубина = глубина + 1;
        } иначе если (байт == целое32(93)) {
            глубина = глубина - 1;
            если (глубина == 0) {
                конец = индекс;
            }
        }
        индекс = индекс + 1;
    }

    если (конец == 0 - 1) {
        вернуть ошибка("не удалось найти конец массива '" + имя_поля + "'");
    }
    вернуть подстрока(json, начало, конец - начало);
}

// Есть ли среди файлов релиза (assets) хотя бы один, чьё имя содержит
// метку текущей платформы — то есть реально ли что-то скачивать именно
// для ЭТОЙ ОС, а не только "номер версии стал больше".
функция релиз_содержит_сборку_для_платформы(текст json) -> верно {
    результат<текст> массив_ассетов = извлечь_json_массив(json, "assets");
    если (!массив_ассетов.успешно) {
        вернуть нет;
    }
    вернуть содержит(массив_ассетов.значение, метка_текущей_платформы());
}

функция проверить_обновления_на_github() -> результат<текст> {
    текст лог = "/tmp/sfera_панель_обновление.log";
    текст команда = "curl -s -H 'Accept: application/vnd.github+json' "
        + "https://api.github.com/repos/nikitosklim/Sfera/releases/latest > " + лог + " 2>/dev/null";
    результат<целое> итог = выполнить_команду(команда);
    если (!итог.успешно) {
        вернуть ошибка("не удалось выполнить запрос (нет интернета?)");
    }

    результат<текст> содержимое = прочитать_файл(лог);
    если (!содержимое.успешно) {
        вернуть ошибка("не удалось прочитать ответ GitHub");
    }
    вернуть успех(содержимое.значение);
}

функция начинается_с(текст строка, текст префикс) -> верно {
    если (длина(строка) < длина(префикс)) {
        вернуть нет;
    }
    результат<текст> начало = подстрока(строка, 0, длина(префикс));
    если (начало.успешно) {
        вернуть начало.значение == префикс;
    }
    вернуть нет;
}

// НОВОЕ: разбирает текстовый вывод "сфера_пакеты список" (см. точный
// формат в показать_список, src/bin/сфера_пакеты.rs) в три параллельных
// списка — по одной записи на пакет. Опирается на точные префиксы строк
// ("  " перед именем, "    источник: "/"    описание: " перед полями) —
// тот же формат, что мы сами и печатаем, поэтому не нужен более общий,
// терпимый к ошибкам разбор.
функция разобрать_пакеты(текст вывод, Список<текст> имена, Список<текст> источники, Список<текст> описания) -> целое {
    текст остаток = вывод;
    текст текущее_имя = "";
    текст текущий_источник = "—";
    текст текущее_описание = "—";
    верно есть_текущий = нет;

    пока (длина(остаток) > 0) {
        текст строка = остаток;
        текст следующий_остаток = "";
        результат<целое> позиция_переноса = найти(остаток, "\n");
        если (позиция_переноса.успешно) {
            результат<текст> часть = подстрока(остаток, 0, позиция_переноса.значение);
            если (часть.успешно) {
                строка = часть.значение;
            }
            целое после = позиция_переноса.значение + 1;
            результат<текст> хвост = подстрока(остаток, после, длина(остаток) - после);
            если (хвост.успешно) {
                следующий_остаток = хвост.значение;
            }
        }

        если (начинается_с(строка, "    источник: ")) {
            // ⚠️ НАЙДЕННЫЙ БАГ: раньше здесь стояло жёсткое число 15 —
            // неверное, поскольку "источник"/"описание" — кириллица,
            // каждая буква 2 байта в UTF-8, реальная длина префикса 22
            // байта. Исправлено на динамический расчёт от самого текста
            // префикса — больше никогда не собьётся, даже если сам текст
            // префикса потом изменится.
            целое длина_префикса = длина("    источник: ");
            результат<текст> значение = подстрока(строка, длина_префикса, длина(строка) - длина_префикса);
            если (значение.успешно) {
                текущий_источник = значение.значение;
            }
        } иначе если (начинается_с(строка, "    описание: ")) {
            целое длина_префикса = длина("    описание: ");
            результат<текст> значение = подстрока(строка, длина_префикса, длина(строка) - длина_префикса);
            если (значение.успешно) {
                текущее_описание = значение.значение;
            }
        } иначе если (начинается_с(строка, "  ") && !начинается_с(строка, "    ") && длина(строка) > 2) {
            // Начало новой записи о пакете — сохраняем предыдущую (если
            // была) и начинаем собирать следующую.
            если (есть_текущий) {
                имена.добавить(текущее_имя);
                источники.добавить(текущий_источник);
                описания.добавить(текущее_описание);
            }
            // Пробелы однобайтовые, "2" тут технически верно, но
            // динамический расчёт для единообразия с исправлением выше.
            целое длина_отступа = длина("  ");
            результат<текст> имя_без_отступа = подстрока(строка, длина_отступа, длина(строка) - длина_отступа);
            если (имя_без_отступа.успешно) {
                текущее_имя = имя_без_отступа.значение;
            }
            текущий_источник = "—";
            текущее_описание = "—";
            есть_текущий = да;
        }

        остаток = следующий_остаток;
        если (!позиция_переноса.успешно) {
            остаток = "";
        }
    }

    если (есть_текущий) {
        имена.добавить(текущее_имя);
        источники.добавить(текущий_источник);
        описания.добавить(текущее_описание);
    }

    вернуть 0;
}

// Выводит многострочный текст (с \n внутри) построчно, начиная с
// заданной Y-координаты — графическая библиотека умеет рисовать только
// одну строку за раз, эта функция разбивает текст сама.
функция нарисовать_многострочный_текст(текст холст, текст шрифт, текст содержимое, целое32 х, целое32 начальный_у) -> целое {
    текст остаток = содержимое;
    целое32 текущий_у = начальный_у;
    целое строк_нарисовано = 0;

    пока (строк_нарисовано < 20) {
        результат<целое> позиция_переноса = найти(остаток, "\n");
        если (позиция_переноса.успешно) {
            результат<текст> строка = подстрока(остаток, 0, позиция_переноса.значение);
            если (строка.успешно) {
                нарисовать_текст(холст, шрифт, строка.значение, х, текущий_у, 4294967295);
            }
            целое следующая_позиция = позиция_переноса.значение + 1;
            // ⚠️ НАЙДЕННАЯ ОШИБКА: третий аргумент "подстрока" — это ДЛИНА
            // подстроки, а не абсолютный индекс конца, как предполагалось
            // изначально. "длина(остаток) - следующая_позиция" —
            // правильная длина оставшейся части СТРОКИ.
            целое длина_остатка = длина(остаток) - следующая_позиция;
            результат<текст> остаток_после = подстрока(остаток, следующая_позиция, длина_остатка);
            если (остаток_после.успешно) {
                остаток = остаток_после.значение;
            } иначе {
                вернуть строк_нарисовано;
            }
        } иначе {
            если (длина(остаток) > 0) {
                нарисовать_текст(холст, шрифт, остаток, х, текущий_у, 4294967295);
                строк_нарисовано = строк_нарисовано + 1;
            }
            вернуть строк_нарисовано;
        }
        текущий_у = текущий_у + целое32(22);
        строк_нарисовано = строк_нарисовано + 1;
    }

    вернуть строк_нарисовано;
}

// НОВОЕ: подробная информация о языке — версия уже была (текущая_версия),
// добавлены название/описание/честные ограничения, показывается на
// вкладке "Обзор".
функция описание_языка() -> текст {
    вернуть "«Сфера» — независимый, компилируемый язык программирования\n"
        + "с полностью русским синтаксисом. Компилятор написан на Rust,\n"
        + "генерирует машинный код напрямую через LLVM.\n\n"
        + "Возможности: автоматическое управление памятью (ARC), контракты\n"
        + "и полиморфизм, обобщённые типы, настоящая многопоточность,\n"
        + "графический интерфейс, собственное хранилище данных.\n\n"
        + "Честные ограничения: соло-проект одного разработчика — скорость\n"
        + "развития такая, какая есть. Нет полноценного пакетного менеджера\n"
        + "с публикацией и версиями. Диагностика ошибок компилятора\n"
        + "активно дорабатывается.";
}

функция главная() -> целое {
    инициализировать(32);
    инициализировать_шрифты();

    текст окно = создать_окно("Сфера — среда разработки", 0, 0, 720, 760, 0);
    текст холст = создать_холст(окно, -1, 0);
    текст шрифт = открыть_шрифт(выбрать_путь_шрифта(), 17);

    // --- Боковая панель вкладок (слева, x = 0..150) ---
    Кнопка вкладка_обзор = Кнопка(0, 0, 150, 50, 45, 45, 50, "");
    Кнопка вкладка_пакеты = Кнопка(0, 50, 150, 50, 45, 45, 50, "");
    Кнопка вкладка_документация = Кнопка(0, 100, 150, 50, 45, 45, 50, "");

    // --- Кнопки вкладки "Обзор" ---
    Кнопка кнопка_установить = Кнопка(170, 200, 220, 50, 60, 160, 60, "");
    Кнопка кнопка_удалить = Кнопка(170, 260, 220, 50, 170, 60, 60, "");
    Кнопка кнопка_собрать = Кнопка(170, 320, 220, 50, 60, 120, 200, "");
    Кнопка кнопка_обновить = Кнопка(170, 380, 260, 50, 200, 140, 40, "");

    // --- Кнопки вкладки "Пакеты" ---
    Кнопка кнопка_список_пакетов = Кнопка(170, 60, 220, 45, 60, 100, 200, "");

    // НОВОЕ: настоящая, структурированная таблица (Имя | Источник |
    // Описание) вместо голого текстового вывода — заполняется разбором
    // вывода "сфера_пакеты список" через разобрать_пакеты.
    Список<целое> ширины_колонок_пакетов = Список<целое>();
    ширины_колонок_пакетов.добавить(150);
    ширины_колонок_пакетов.добавить(160);
    ширины_колонок_пакетов.добавить(240);
    Таблица таблица_пакетов = Таблица(170, 130, ширины_колонок_пакетов, 30);

    Список<текст> заголовки_пакетов = Список<текст>();
    заголовки_пакетов.добавить("Имя");
    заголовки_пакетов.добавить("Источник");
    заголовки_пакетов.добавить("Описание");

    Список<текст> имена_пакетов = Список<текст>();
    Список<текст> источники_пакетов = Список<текст>();
    Список<текст> описания_пакетов = Список<текст>();

    // --- Кнопки вкладки "Документация" ---
    Кнопка кнопка_открыть_документацию = Кнопка(170, 300, 260, 50, 60, 160, 60, "");

    целое текущая_вкладка = 0;
    текст статус = "";
    верно найдено_обновление = нет;
    текст новая_версия_текст = "";
    // НОВОЕ: полный текст описания изменений найденного обновления —
    // из поля "body" ответа GitHub Releases API, показывается
    // многострочно на экране, когда найдено_обновление == да.
    текст новая_версия_описание = "";
    // НОВОЕ: папка установки — по умолчанию прежнее значение (~/.sfera),
    // меняется на выбранную пользователем через системный диалог при
    // нажатии "Установить" (см. выбрать_папку_через_диалог).
    текст выбранная_папка_установки = папка_установки_по_умолчанию();
    верно продолжать = да;
    текст буфер_события = выделить_память(64);
    целое итерация = 0;

    пока (продолжать && итерация < 6000) {
        целое32 есть_событие = опросить_событие(буфер_события);
        если (есть_событие != целое32(0)) {
            целое32 тип_события = прочитать_целое32(буфер_события, 0);

            если (тип_события == целое32(256)) {
                продолжать = нет;
            } иначе если (тип_события == целое32(1025)) {
                целое32 х = прочитать_целое32(буфер_события, 20);
                целое32 у = прочитать_целое32(буфер_события, 24);

                // --- Переключение вкладок — работает независимо от того,
                // какая вкладка сейчас открыта ---
                если (вкладка_обзор.содержит_точку(х, у)) {
                    текущая_вкладка = 0;
                    статус = "";
                } иначе если (вкладка_пакеты.содержит_точку(х, у)) {
                    текущая_вкладка = 1;
                    статус = "";
                } иначе если (вкладка_документация.содержит_точку(х, у)) {
                    текущая_вкладка = 2;
                    статус = "";
                } иначе если (текущая_вкладка == 0) {
                    если (!сфера_уже_установлена(выбранная_папка_установки) && кнопка_установить.содержит_точку(х, у)) {
                        статус = "Выбери папку для установки...";
                        результат<текст> выбор = выбрать_папку_через_диалог();
                        если (!выбор.успешно) {
                            статус = "Установка отменена.";
                        } иначе {
                            выбранная_папка_установки = выбор.значение;
                            статус = "Устанавливаю в " + выбранная_папка_установки + "...";
                            верно установлено_успешно = установить_сферу(выбранная_папка_установки);
                            если (установлено_успешно) {
                                статус = "Готово — Сфера установлена.";
                            } иначе {
                                статус = "Не удалось установить.";
                            }
                        }
                    } иначе если (сфера_уже_установлена(выбранная_папка_установки) && кнопка_удалить.содержит_точку(х, у)) {
                        статус = "Удаляю...";
                        верно удалено_успешно = удалить_сферу(выбранная_папка_установки);
                        если (удалено_успешно) {
                            статус = "Сфера удалена.";
                        } иначе {
                            статус = "Не удалось удалить.";
                        }
                    } иначе если (сфера_уже_установлена(выбранная_папка_установки) && кнопка_собрать.содержит_точку(х, у)) {
                        статус = "Идёт сборка...";
                        текст лог_ошибки = "/tmp/sfera_панель_ошибка.log";
                        текст команда_сборки = "cd /tmp && " + путь_установленного_sfera(выбранная_папка_установки) + " " + путь_установленного_примера(выбранная_папка_установки) + " -c 2> " + лог_ошибки;
                        результат<целое> итог = выполнить_команду(команда_сборки);
                        если (итог.успешно && итог.значение == 0) {
                            статус = "Сборка успешна!";
                        } иначе {
                            результат<текст> содержимое_ошибки = прочитать_файл(лог_ошибки);
                            если (содержимое_ошибки.успешно && длина(содержимое_ошибки.значение) > 0) {
                                целое показать_символов = 60;
                                если (длина(содержимое_ошибки.значение) < показать_символов) {
                                    показать_символов = длина(содержимое_ошибки.значение);
                                }
                                результат<текст> обрезанная_ошибка = подстрока(содержимое_ошибки.значение, 0, показать_символов);
                                если (обрезанная_ошибка.успешно) {
                                    статус = "Ошибка: " + обрезанная_ошибка.значение;
                                }
                            } иначе {
                                статус = "Ошибка сборки.";
                            }
                        }
                    } иначе если (кнопка_обновить.содержит_точку(х, у)) {
                        если (найдено_обновление) {
                            // НОВОЕ: вместо автоматической подмены самого
                            // .app-бандла (рискованно — панель заменяла бы
                            // файлы, из которых сама сейчас запущена) —
                            // честно, безопасно открываем страницу релиза
                            // на GitHub в браузере, пользователь сам
                            // скачивает и заменяет бандл, обычным macOS-
                            // способом обновления приложений.
                            выполнить_команду("open https://github.com/nikitosklim/Sfera/releases/latest");
                            статус = "Открыл страницу релиза в браузере — скачай и замени приложение.";
                        } иначе {
                            статус = "Проверяю обновления...";
                            результат<текст> ответ = проверить_обновления_на_github();
                            если (!ответ.успешно) {
                                статус = "Не удалось проверить обновления: " + ответ.сообщение;
                            } иначе {
                                текст json = ответ.значение;
                                результат<текст> тег = извлечь_json_строковое_поле(json, "tag_name");
                                если (!тег.успешно) {
                                    статус = "Не удалось разобрать ответ GitHub (нет релизов?).";
                                } иначе {
                                    текст версия_с_github = тег.значение;
                                    если (начинается_с(версия_с_github, "v")) {
                                        результат<текст> без_v = подстрока(версия_с_github, 1, длина(версия_с_github) - 1);
                                        если (без_v.успешно) {
                                            версия_с_github = без_v.значение;
                                        }
                                    }

                                    если (версия_с_github == текущая_версия()) {
                                        статус = "У тебя последняя версия.";
                                    } иначе если (!релиз_содержит_сборку_для_платформы(json)) {
                                        // НОВОЕ: версия на GitHub новее, но
                                        // среди файлов релиза нет сборки
                                        // под ЭТУ платформу (метка_текущей_
                                        // платформы) — честно, отдельно
                                        // сообщаем об этом, не предлагаем
                                        // "обновить" то, чего фактически
                                        // нет для скачивания.
                                        статус = "Вышла версия " + версия_с_github + ", но пока не собрана для " + метка_текущей_платформы() + ".";
                                    } иначе {
                                        найдено_обновление = да;
                                        новая_версия_текст = версия_с_github;
                                        новая_версия_описание = "Без описания изменений.";
                                        результат<текст> тело = извлечь_json_строковое_поле(json, "body");
                                        если (тело.успешно && длина(тело.значение) > 0) {
                                            новая_версия_описание = разэкранировать_json_текст(тело.значение);
                                        }
                                        статус = "Доступно обновление " + версия_с_github + "!";
                                    }
                                }
                            }
                        }
                    }
                } иначе если (текущая_вкладка == 1) {
                    если (кнопка_список_пакетов.содержит_точку(х, у)) {
                        статус = "Обновляю список...";
                        имена_пакетов = Список<текст>();
                        источники_пакетов = Список<текст>();
                        описания_пакетов = Список<текст>();
                        разобрать_пакеты(получить_список_пакетов(выбранная_папка_установки), имена_пакетов, источники_пакетов, описания_пакетов);
                        статус = "";
                    }
                } иначе если (текущая_вкладка == 2) {
                    если (кнопка_открыть_документацию.содержит_точку(х, у)) {
                        открыть_документацию();
                        статус = "Открываю README.md...";
                    }
                }
            }
        }

        // --- Отрисовка ---
        задать_цвет(холст, 20, 20, 20, 255);
        очистить_холст(холст);

        // Боковая панель — фон чуть светлее основного, для визуального
        // разделения (в духе таблицы с "шапкой"/колонкой заголовков).
        Кнопка фон_боковой_панели = Кнопка(0, 0, 150, 760, 30, 30, 35, "");
        фон_боковой_панели.нарисовать(холст, шрифт);

        вкладка_обзор.нарисовать(холст, шрифт);
        вкладка_пакеты.нарисовать(холст, шрифт);
        вкладка_документация.нарисовать(холст, шрифт);
        нарисовать_текст(холст, шрифт, "Обзор", 15, 18, 4294967295);
        нарисовать_текст(холст, шрифт, "Пакеты", 15, 68, 4294967295);
        нарисовать_текст(холст, шрифт, "Документация", 15, 118, 4294967295);

        если (текущая_вкладка == 0) {
            нарисовать_текст(холст, шрифт, "Обзор языка Сфера", 170, 20, 4294967295);
            нарисовать_текст(холст, шрифт, "Версия: " + текущая_версия(), 170, 50, 4294967295);

            если (сфера_уже_установлена(выбранная_папка_установки)) {
                нарисовать_текст(холст, шрифт, "Статус: установлено", 170, 80, 4294967295);
                нарисовать_текст(холст, шрифт, "Расположение: " + выбранная_папка_установки, 170, 105, 4294967295);
                кнопка_удалить.нарисовать(холст, шрифт);
                нарисовать_текст(холст, шрифт, "Удалить", 185, 275, 4294967295);
                кнопка_собрать.нарисовать(холст, шрифт);
                нарисовать_текст(холст, шрифт, "Собрать привет.код", 185, 335, 4294967295);
                кнопка_обновить.нарисовать(холст, шрифт);
                если (найдено_обновление) {
                    нарисовать_текст(холст, шрифт, "Установить обновление", 185, 395, 4294967295);
                } иначе {
                    нарисовать_текст(холст, шрифт, "Проверить обновления", 185, 395, 4294967295);
                }
            } иначе {
                нарисовать_текст(холст, шрифт, "Статус: не установлено", 170, 80, 4294967295);
                кнопка_установить.нарисовать(холст, шрифт);
                нарисовать_текст(холст, шрифт, "Установить Сферу", 185, 215, 4294967295);
            }

            // НОВОЕ: подробная информация внизу — при найденном обновлении
            // показываем ЕГО описание (то, что реально сейчас важно
            // пользователю), иначе — общее описание языка и его честные
            // ограничения.
            если (найдено_обновление) {
                нарисовать_текст(холст, шрифт, "Что нового в версии " + новая_версия_текст + ":", 170, 450, 4294961664);
                нарисовать_многострочный_текст(холст, шрифт, новая_версия_описание, 170, 475);
            } иначе {
                нарисовать_многострочный_текст(холст, шрифт, описание_языка(), 170, 450);
            }
        } иначе если (текущая_вкладка == 1) {
            нарисовать_текст(холст, шрифт, "Установленные пакеты", 170, 20, 4294967295);
            кнопка_список_пакетов.нарисовать(холст, шрифт);
            нарисовать_текст(холст, шрифт, "Обновить список", 185, 75, 4294967295);

            если (имена_пакетов.размер() > 0) {
                таблица_пакетов.нарисовать_заголовок(холст, шрифт, заголовки_пакетов);
                целое32 строка_индекс = целое32(0);
                пока (целое(строка_индекс) < имена_пакетов.размер()) {
                    Список<текст> строка_значений = Список<текст>();
                    строка_значений.добавить(имена_пакетов.получить(целое(строка_индекс)));
                    строка_значений.добавить(источники_пакетов.получить(целое(строка_индекс)));
                    строка_значений.добавить(описания_пакетов.получить(целое(строка_индекс)));
                    верно чётная_строка = целое(строка_индекс) % 2 == 0;
                    таблица_пакетов.нарисовать_строку(холст, шрифт, строка_индекс + целое32(1), строка_значений, чётная_строка);
                    строка_индекс = строка_индекс + целое32(1);
                }
            } иначе {
                нарисовать_текст(холст, шрифт, "Нажми «Обновить список», чтобы увидеть пакеты.", 170, 130, 4282729797);
            }
        } иначе если (текущая_вкладка == 2) {
            нарисовать_текст(холст, шрифт, "Документация", 170, 20, 4294967295);
            нарисовать_текст(холст, шрифт, "Язык программирования «Сфера» — полностью", 170, 55, 4294967295);
            нарисовать_текст(холст, шрифт, "кириллический синтаксис, компиляция в машинный", 170, 77, 4294967295);
            нарисовать_текст(холст, шрифт, "код через LLVM, автоматический подсчёт ссылок (ARC).", 170, 99, 4294967295);
            нарисовать_текст(холст, шрифт, "Полное описание возможностей — в файле README.md", 170, 130, 4294967295);
            нарисовать_текст(холст, шрифт, "в папке проекта.", 170, 152, 4294967295);
            кнопка_открыть_документацию.нарисовать(холст, шрифт);
            нарисовать_текст(холст, шрифт, "Открыть README", 185, 315, 4294967295);
        }

        нарисовать_текст(холст, шрифт, статус, 170, 720, 4294967295);
        показать_холст(холст);

        итерация = итерация + 1;
        подождать(20);
    }

    закрыть_окно(окно);
    завершить();

    вернуть 0;
}
