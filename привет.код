внешняя функция подождать_завершения_демо_потока(беззнак32 секунды) как "sleep";

структура Точка {
    целое х;
    целое у;
}

структура Держатель {
    текст имя;
    слабая Точка цель;
}

функция главная() -> целое {
    вывести("Привет, мир!");
    вернуть 0;
}
