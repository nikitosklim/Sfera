подключить модуль_а;
подключить модуль_б;

функция главная() -> целое {
    // Раньше (v1) это была бы ошибка компиляции ("функция объявлена
    // повторно") — теперь коллизия разрешена, доступ строго через
    // квалификацию.
    вывести(модуль_а.помощь());
    вывести(модуль_б.помощь());
    вернуть 0;
}
